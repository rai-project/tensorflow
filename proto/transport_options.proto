syntax = "proto3";

package tensorflow;
option go_package = "github.com/rai-project/tensorflow";

// Extra data needed on a non-RDMA RecvBufResponse.
message RecvBufRespExtra {
  bytes tensor_content = 1;
};
